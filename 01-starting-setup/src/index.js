import ReactDOM from 'react-dom';

import './index.css';
import App from './App'; //* we need to use App.js if it was from an external source.

ReactDOM.render(<App />, document.getElementById('root'));
